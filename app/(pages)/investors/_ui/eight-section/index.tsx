import { images } from "@/app/(pages)/in-the-media/_const/carousel-images"
import { Carousel } from "@/app/(app)/_components/carousel"

export const EightSection = () => {
  return <Carousel imagesSrc={images} />
}
